import React from 'react';
import PageBlank from '../components/PageBlank';

const Cart = ({ setMenuOpen, menuOpen }) => (
  <PageBlank setMenuOpen={setMenuOpen} menuOpen={menuOpen} labelText="Your Cart"/>
);

export default Cart;
