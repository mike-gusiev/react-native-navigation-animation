import React from 'react';
import PageBlank from '../components/PageBlank';

const Favorites = ({ setMenuOpen, menuOpen }) => (
  <PageBlank setMenuOpen={setMenuOpen} menuOpen={menuOpen} labelText="Favorites"/>
);

export default Favorites;
