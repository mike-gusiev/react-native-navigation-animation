import React from 'react';
import PageBlank from '../components/PageBlank';

const Orders = ({ setMenuOpen, menuOpen }) => (
  <PageBlank setMenuOpen={setMenuOpen} menuOpen={menuOpen} labelText="Orders"/>
);

export default Orders;
