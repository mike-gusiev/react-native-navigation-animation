import React from 'react';
import PageBlank from '../components/PageBlank';

const SignOut = ({ setMenuOpen, menuOpen }) => (
  <PageBlank setMenuOpen={setMenuOpen} menuOpen={menuOpen} labelText="Sign Out"/>
);

export default SignOut;
