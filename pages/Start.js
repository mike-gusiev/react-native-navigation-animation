import React from 'react';
import PageBlank from '../components/PageBlank';

const Start = ({ setMenuOpen, menuOpen }) => (
  <PageBlank setMenuOpen={setMenuOpen} menuOpen={menuOpen} label="start" labelText="START"/>
);

export default Start;
